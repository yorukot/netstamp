# Database Migrations

Add versioned Goose SQL migrations here when the first real PostgreSQL schema change is introduced.

Use timestamped filenames such as:

```text
202604300001_create_example_table.sql
```

Keep schema changes forward-compatible when they are deployed with application changes.
